# v1.5.0 Direct Merge Plan

## Goal

在不继续使用 GitHub PR 审批流的前提下，把 `release/v1.5.0-prep-2026-05-15` 这条已经收口的 release-prep 分支直接合并回本地 `master`，并推送到 `origin/master`，同时保留完整的文档化决策与外部阻塞说明。

## Architecture

- 当前承载分支：`release/v1.5.0-prep-2026-05-15`
- 当前目标分支：本地 `master`
- 当前默认真相链：`docs/ROADMAP.md` -> `docs/plans/2026-05-12-release-v1.5.0-formalization.md` -> `docs/test_reports/RELEASE_READINESS_V1.5.0.md` -> `.github/README.md`
- 当前 direct-merge 决策：
  - 用户明确选择不继续走 PR
  - 已创建的 `#13` 仅保留为历史记录，关闭而不合并
- 本批只允许修改：
  - direct-merge 计划文档
  - PR approval packet 的最终状态
  - 根 working-memory 文件
- 本批不允许：
  - 创建 `v1.5.0` tag
  - 发布 GitHub Release
  - 伪装 Windows/WinSSL runtime 证据已补齐
  - 把 GitHub Actions billing 问题误写成代码回归

## Files

- Add: `docs/plans/2026-05-15-v1.5.0-direct-merge.md`
- Update: `docs/test_reports/PR_APPROVAL_PACKET_V1.5.0_2026-05-15.md`
- Update: `task_plan.md`
- Update: `findings.md`
- Update: `progress.md`

## Task 1: Close The PR Route

Run:

```bash
gh pr close 13 --comment "Superseded by the user-approved direct merge route. We are closing this PR and merging the release-prep branch locally into master instead."
gh pr view 13 --json number,title,state,url,headRefName,baseRefName
```

Expected:

- PR `#13` is `CLOSED`
- no merged PR remains as the active route

## Task 2: Rewrite Control-Plane Metadata

Write:

- direct-merge plan
- approval packet final state as historical / closed
- root working-memory status, queue, and blocker wording for direct merge
- explicit reminder that GitHub Actions billing failures can still affect push-triggered workflows after merge

## Task 3: Focused Verification

Run from repo root:

```bash
bash tests/scripts/test_release_control_entrypoint_convergence_contract.sh
bash tests/scripts/test_active_roadmap_references_contract.sh
bash tests/scripts/test_release_workflow_v1_5_0_contract.sh
git diff --check
git status --short
```

Expected:

- release-control docs still converge
- roadmap truth still converges
- release workflow contract still passes
- only expected doc changes remain

## Task 4: Commit And Push The Metadata Batch

- summarize a short review conclusion before commit
- commit the direct-merge metadata batch on `release/v1.5.0-prep-2026-05-15`
- push the branch so the closed PR history and branch tip match the repo docs

## Task 5: Merge Into master

Run:

```bash
git switch master
git merge --no-ff release/v1.5.0-prep-2026-05-15 -m "merge: finalize v1.5.0 direct merge route"
git status --short --branch
git push origin master
```

Expected:

- merge commit is created locally on `master`
- `origin/master` receives the release-prep closure history
- branch stays available as historical release-prep evidence unless explicitly deleted later

## Stop Condition

- PR `#13` 已关闭且未合并
- focused contract checks 继续通过
- direct-merge metadata commit 已创建并推送到 `origin/release/v1.5.0-prep-2026-05-15`
- `master` merge commit 已创建并推送到 `origin/master`
- 根 working-memory 已记录 direct merge 路线与外部 GitHub Actions blocker
