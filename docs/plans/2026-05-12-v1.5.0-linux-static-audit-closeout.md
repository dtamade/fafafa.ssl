# v1.5.0 Linux Static Audit Closeout

## Summary

This batch closes `v1.5.0` on the Linux host only. The goal is to finish release readiness with:

- Linux gates green
- Pascal surface audited for completeness
- release documentation aligned to the actual closeout policy
- final merge back into `main`

Windows/GitHub runtime proof is intentionally deferred because the current release scope is Linux-only and remote quota is unavailable.

## Key Changes

- Add a reproducible static audit contract for the Pascal public surface and placeholder scan.
- Update release notes and readiness docs so they describe the Linux-only closeout accurately.
- Record the static audit result in `docs/test_reports/STATIC_AUDIT_V1.5.0.md`.
- Merge the verified release branch back into `main`.

## Test Plan

Run the existing Linux gates first:

- `python3 scripts/compile_all_modules.py`
- `bash scripts/run_minimal_ci_gate.sh --fast-local`
- `bash scripts/run_freepascal_tls13_completeness_gate.sh --fast-local --run-id release_1_5_0_20260512`
- `python3 scripts/check_code_style.py src`
- `bash scripts/run_phase2_performance_baseline.sh --dry-run --fast-local`
- `bash tests/scripts/test_release_workflow_v1_5_0_contract.sh`

Then run the static Pascal audit contract:

- `bash tests/scripts/test_v1_5_0_static_pascal_audit_contract.sh`

Finish with hygiene checks and merge the branch into `main` after review.

## Assumptions

- `v1.5.0` stays the release target.
- The Linux release closeout does not depend on fresh GitHub Actions runtime artifacts.
- `TSSLHelper` remains a public helper class; only old global helper aliases/functions are treated as removed/deprecated release surface.
- Any future Windows runtime proof will be handled as a separate follow-up batch, not as a blocker for this closeout.
