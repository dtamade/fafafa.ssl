FROM ubuntu:22.04

# Install FPC and dependencies
RUN apt-get update && apt-get install -y \
    fpc \
    fp-compiler \
    fp-units-* \
    libssl-dev \
    make \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

# Default command
CMD ["bash"]
